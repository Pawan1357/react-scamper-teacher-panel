import styled from 'styled-components';

export const ManageChapterWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 15px;

  .avatar-contain img {
    object-fit: contain;
  }
`;
